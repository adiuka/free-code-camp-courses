Design a Feature Selection Page

Build an app that is functionally similar to this example project. Try not to copy the example project, give it your own personal style.

Objective: Fulfill the user stories below and get all the tests to pass to complete the lab.

User Stories:

    Your page should have an h1 element with the text Feature Selection.
    Your page should have a div element with the class feature-card-container.
    Your page should have at least two label elements each with the class feature-card inside the div.
    Each of your label elements should have label text and an input element of type checkbox in them.
    Create a selector that targets the checkboxes, and apply the following styling:
        All of your checkbox elements should be set to appearance: none; in your CSS.
        All of your checkbox elements should have a border width, color and style of your choosing.
    When the checkbox is checked, it should display a checkmark ✓ in the center of the checkbox.
    When the checkbox is checked, the background color of the checkbox should change to a color of your choosing.
    When the checkbox is checked, the border color of the checkbox should change to a color of your choosing.

Note: Be sure to link your CSS file in your HTML.
Tests:

    Waiting: 1. You should have an h1 element.
    Waiting: 2. Your h1 should have a text of Feature Selection.
    Waiting: 3. You should have a div element with the class feature-card-container.
    Waiting: 4. You should have at least two label elements inside .feature-card-container.
    Waiting: 5. Each label element inside .feature-card-container should have the class feature-card.
    Waiting: 6. Inside each label element you should have label text.
    Waiting: 7. Inside each label element there should be one input element of type checkbox.
    Waiting: 8. You should have a selector for the checkboxes.
    Waiting: 9. Your checkboxes should be set to appearance: none.
    Waiting: 10. Your checkboxes should have a border width of your choosing.
    Waiting: 11. Your checkboxes should have a border color of your choosing.
    Waiting: 12. Your checkboxes should have a border style of your choosing.
    Waiting: 13. When the checkboxes are selected, there should be a checkmark ✓ in the center of the checkbox.
    Waiting: 14. When the checkbox is checked, the background color of the checkbox should change to a color of your choosing.
    Waiting: 15. When the checkbox is checked, the border color of the checkbox should change to a color of your choosing.